/**
 * This file is executed automatically before running any Jest test.
 * Use it to configure the global test environment.
 *
 * What you SHOULD put here:
 * - Global mocks (e.g. fetch, localStorage, IntersectionObserver, ResizeObserver)
 * - Third-party library mocks that must be available for all tests
 * - Jest configuration that applies globally (e.g. jest.spyOn(...) overrides)
 * - Polyfills required by the test environment
 *
 * What you SHOULD NOT put here:
 * - Component-specific mocks (these belong in the test file itself)
 * - Test cases or assertions
 *
 * Keep this file lightweight: only global setup belongs here.
 */
